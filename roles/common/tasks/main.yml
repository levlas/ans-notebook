---

- name: install packages
  apt: name="{{item}}" state=present
  with_items:
    - vim
    - curl
    # ...
    
- name: install an Ubuntu desktop before Pantheon
  apt: name="ubuntu-desktop" install-recommends=yes state=present

- include: mono.yml
- include: pantheon.yml
